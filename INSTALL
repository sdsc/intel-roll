This roll bundles the Intel compilers.  To install, execute these instructions
on a Rocks development machine (e.g., a frontend), starting in this directory:

% make
% rocks add roll *.iso
% rocks enable roll intel
% cd /export/rocks/install
% rocks create distro
% rocks run roll intel | bash

In addition to the software itself, the roll installs Intel environment module
files in /opt/modulefiles/compilers/intel.
